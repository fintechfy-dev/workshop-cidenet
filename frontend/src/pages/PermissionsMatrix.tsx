// Pantalla 3: Matriz de permisos (ver specs/BRIEF.md, seccion 4 y 5).
// TODO (ejercicio del taller): grid recurso x rol con toggles CRUD,
// solo editable por admin, conectado al API real.
export function PermissionsMatrix() {
  return (
    <section>
      <h2>Matriz de Permisos</h2>
      <p>TODO: construir esta pantalla durante el taller (iteracion de frontend).</p>
    </section>
  );
}
