// Pantalla 1: Tabla de usuarios (ver specs/BRIEF.md, seccion 5).
// TODO (ejercicio del taller): tabla paginada con busqueda, filtro por rol/estado,
// y acciones por fila, conectada al API real.
export function UsersTable() {
  return (
    <section>
      <h2>Usuarios</h2>
      <p>TODO: construir esta pantalla durante el taller (iteracion de frontend).</p>
    </section>
  );
}
